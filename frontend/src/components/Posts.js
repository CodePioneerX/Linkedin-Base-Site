import React from 'react'
import { Alert } from 'react-bootstrap'

function Posts() {
  return (
    // <Alert variant={variant}>
    //     {children}
    // </Alert>
    <div>WASSIM</div>
  )
}

export default Posts